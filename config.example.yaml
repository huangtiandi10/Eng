ai:
  provider: openai
  base_url: https://api.openai.com/v1
  api_key: ""
  model: gpt-4o-mini
  timeout_seconds: 60
study:
  daily_new_words: 30
  daily_reviews: 50
  daily_phrases: 10
  daily_listening: 5
  exam_date: ""
server:
  # Mac 本机使用 127.0.0.1；需要让 Windows 访问时改为 0.0.0.0。
  host: 127.0.0.1
  port: 8765
  # 首次启动会自动生成；不要提交真实令牌到 Git。
  access_token: ""
